{
  "version": 1,
  "routes": [
    {
      "source": "/api/*",
      "target": "/artifacts/compute/default",
      "handler": "server/index.handler",
      "environment": {
        "NODE_ENV": "production"
      }
    },
    {
      "source": "/_health",
      "target": "/artifacts/compute/default",
      "handler": "server/index.handler",
      "environment": {
        "NODE_ENV": "production"
      }
    }
  ]
} 